import { GrocyImagePipe } from './grocy-image.pipe';

describe('GrocyImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GrocyImagePipe();
    expect(pipe).toBeTruthy();
  });
});
